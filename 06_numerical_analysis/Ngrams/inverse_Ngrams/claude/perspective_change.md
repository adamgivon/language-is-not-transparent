1. Persistent memory saved: scale_calibration.md is now in local Claude memory directory (not published) with the actual token volumes, the percentage conversions for
  every finding, the language calibration rules, and guidance for what to rewrite. It's indexed in MEMORY.md. This survives compaction.
  2. key_findings.md is older (before the scale correction) — but the new scale_calibration.md supersedes it where they conflict. The MEMORY.md index flags scale_calibration as CRITICAL.
  3. One calibrated summary written: system_signatures_summary_calibrated.md — this is the most important document (the paper-ready one). It has:
    - Scale context with actual token totals
    - All findings as percentages
    - Language softened ("mild comparative tendencies" not "signatures")
    - Distribution finding foregrounded as primary evidence
    - Honest paper-ready claim at the end
  4. Remaining documents need rewriting but the pattern is clear from the calibrated summary:
    - phase3_observations.md
    - negative_space_observations.md
    - synthesis_lexicon_vs_negative_space.md
    - shared_space_2_3_systems/observations.md + phrase_content_glance.md
    - Control_signature/summaries/observations_and_conclusions_summary.md

  Each should follow the same recalibration: state token volumes, express findings as percentages, soften language, foreground structural findings.

  If context allows in this session or next: work through the remaining documents using scale_calibration.md as the master reference. The calibrated summary I just wrote is the template.

  If context runs out: the memory note is the recovery path. Next session can start by reading scale_calibration.md and applying it to the remaining documents.
