# Control Signature Summary With Proportions

## What this summary does

This note rewrites the `CONTROL` signature read with explicit denominator framing.

The main result still holds:
- `CONTROL` does have a recognizable style
- but its clearest signal is phrase shape, not a narrow vocabulary identity

The scale must still be stated clearly.

This section should also be read in the larger context:
- the main n-gram result is still large-scale separation between `CONTROL` and the anchored systems
- the `CONTROL` signature layer does not weaken that conclusion
- it only adds a restrained description of what the baseline looks like once the larger divergence has already been established

## Scale first

The strongest recurring `CONTROL` phrase starters remain small as a share of total trigram + quadgram windows, even though they are stable:

- `if you`: `220` = `0.2108%` of total tri+quad windows (`21.078` per `10k`)
- `it s`: `164` = `0.1571%` (`15.712` per `10k`)
- `you can`: `156` = `0.1495%` (`14.946` per `10k`)
- `you re`: `111` = `0.1063%` (`10.635` per `10k`)
- `don t`: `107` = `0.1025%` (`10.251` per `10k`)

So even the strongest phrase starter does not dominate total output. What matters is not size alone, but cross-scenario stability plus recurrence.

The word layer is even more diffuse. Some top disproportionate words are:

- `shared`: `203` = `0.1945%`
- `people`: `182` = `0.1744%`
- `recommend`: `154` = `0.1475%`
- `consequence`: `126` = `0.1207%`
- `realistically`: `91` = `0.0872%`

These are not large enough to define `CONTROL` by themselves.

## What the control-signature layer still shows

The repeated phrase starters are stable across scenarios. This is where the strongest `CONTROL` signal lives.

The most repeated total phrases include:

- `if you can`
- `if you want`
- `you can t`
- `you don t`
- `you tell me`
- `if you tell me`
- `i can help you`

This is enough to support a careful statement:
- `CONTROL` repeatedly builds answers through user-facing, conditional, advisory scaffolds

That is a phrase-shape claim, not a whole-system personality claim.

## Word layer

The disproportionate word layer points in the same direction, but more weakly.

It suggests a broad lean toward:

- evaluation
- recommendation
- consequence framing
- planning or practical guidance
- interpersonal framing in `S7`

But the word layer is partly scenario-sensitive, and each individual word remains a small share of total output.

So the safe use of the word layer is:
- support for the phrase-layer reading
- not a standalone vocabulary-identity claim

## What the control summary supports

The `CONTROL` section supports claims of this kind:

- `CONTROL` is not patternless
- its most stable signal is repeated advisory framing
- it is easier to identify through recurrent phrase structure than through a sharp inverse vocabulary signature

## What it does not support

It does not support claims of this kind:

- `CONTROL` has a large dominant lexical signature
- one or two words define the baseline
- the `CONTROL` style occupies a large share of total output

## Bottom line

The safest proportional reading is:

`CONTROL` has a real signature, but it is a light recurrent frame rather than a heavy lexical identity. Its clearest signal is repeated advisory and explanatory phrase shape, visible across scenarios but still small in absolute mass against total trigram + quadgram output.
